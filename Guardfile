# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'coffeescript', :input => 'src', :output => 'src', :bare => true
guard 'coffeescript', :input => 'test', :output => 'test', :bare => true
